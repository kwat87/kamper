[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @kamper/shared build",
  "pnpm --filter @kamper/web build"
]

[start]
cmd = "pnpm --filter @kamper/web start"
