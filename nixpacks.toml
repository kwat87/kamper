[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @kamper/shared build", "pnpm --filter @kamper/web build"]

[start]
cmd = "pnpm --filter @kamper/web start"
